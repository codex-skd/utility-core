<h2>v1.11.8 - Mixin Verification</h2>

<h3>Diagnostic</h3>
<ul>
<li><strong>Added verification logging for mixin injection</strong>: Testing whether the <code>slotChangedCraftingGrid</code> mixin is actually being invoked on the server when ingredients change. If this isn't being called, the entire recipe list never gets initialized in <code>PlayerRecipeData</code>, which explains why recipe selection always fails.</li>
</ul>

<hr>

<p><strong>JAR</strong>: <code>utility_core-26.2-neoforge-1.11.8.jar</code></p>
