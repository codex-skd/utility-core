<h2>v1.1.2 - Diagnostics &amp; reliability</h2>

<h3>Fix</h3>
<ul>
<li><strong>ChunkGen resume</strong>: generation now correctly restarts after loading saved state from disk. Previously <code>start()</code> never set <code>running = true</code> when dimensions already existed in the saved map.</li>
</ul>

<h3>Technical Changes</h3>
<ul>
<li><code>EnderDragonRespawnHandler</code> — added detailed per-step logging (portal center, bedrock scan, fight stage, crystal placement) to diagnose respawn stalls.</li>
<li><code>tryRespawn()</code> wrapped in try/catch to capture silent failures.</li>
<li>IDE and cache files (<code>.eclipse/</code>, <code>.gradle/</code>, <code>.vscode/</code>) removed from git tracking.</li>
<li>Workspace structure aligned with workflow v1.6.0.</li>
</ul>
