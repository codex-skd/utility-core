<h2>v1.3.0 - Title Position Fix</h2>

<h3>Fix</h3>
<ul>
<li><strong>Titles no longer block the view</strong>: vanilla title/subtitle text (biome/dimension titles, <code>/title</code>, achievements) rendered dead-center on screen, right over the crosshair. Now shifted up via a new <code>titleVerticalOffset</code> setting (default 75px, range -200 to 200).</li>
</ul>

<hr>

<p><strong>JAR</strong>: <code>utility_core-26.2-neoforge-1.3.0.jar</code></p>
