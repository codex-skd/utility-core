# CLAUDE.md — utility_core (26.2)

Este repositorio es un mod NeoForge del grupo `stalking-dragons/minecraft`.

## Paso 0 obligatorio

1. Lee `../../codex-docs/WORKFLOW_AGENT.md` antes de tocar cualquier cosa — es el punto de entrada obligatorio para trabajar en un mod de este grupo.
   - Si esa ruta no existe (este repo se clonó de forma aislada, sin el resto de `Mods_Minecraft` al lado), **pregunta al usuario** dónde está `codex-docs` antes de continuar. No asumas convenciones por tu cuenta.
2. `WORKFLOW_AGENT.md` remite a `codex-docs/WORKFLOW_GENERIC.md` (convenciones, ramas, versionado, CI/CD, formato CurseForge) y al workflow propio de este mod: `docs/WORKFLOW_UTILITY_CORE_26-2.md`. Léelos en ese orden.
3. Las reglas generales (idioma, no asumir información, no borrar sin permiso, prioridad de instrucciones, etc.) están en `../../codex-docs/reference/CLAUDE.md`. Ese archivo manda salvo que el usuario indique lo contrario en esta sesión.

## Prioridad de instrucciones

1. Petición del usuario en esta sesión.
2. Este archivo + `codex-docs/reference/CLAUDE.md`.
3. Workflow del proyecto (`WORKFLOW_AGENT.md` → `WORKFLOW_GENERIC.md` → `docs/WORKFLOW_UTILITY_CORE_26-2.md`).
4. Convenciones existentes del proyecto.
