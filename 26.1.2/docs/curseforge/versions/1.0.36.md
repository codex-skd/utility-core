<h2>v1.0.36 - Ender Dragon Respawn: YUNG Exit Portal Position</h2>

<h3>Fix</h3>
<ul>
<li><strong>Dragon respawn with YUNG Better End Island</strong>: Crystals are now placed at distance 7 from the actual exit portal position (obtained via <code>EnderDragonFightAccessor.getPortalLocation()</code>), on valid bedrock with <code>setBeamTarget</code>.</li>
<li><strong>Bedrock verification</strong>: Ensures each crystal sits on bedrock with air above, matching the requirements of both YUNG BEI and vanilla end island.</li>
</ul>

<h3>Technical Changes</h3>
<ul>
<li><code>EnderDragonRespawnHandler</code> &mdash; Refactored: tries YUNG distance 7 from portal center first, then vanilla distance 2 from (0,60,0), then fallback distances 3-6.</li>
<li>Detects YUNG accessor via reflection: <code>getPortalLocation()</code> returns the actual portal position used by YUNG's dragon fight mixin.</li>
</ul>

<hr>

<p><strong>JAR</strong>: <code>utility_core-26.1.2-neoforge-1.0.36.jar</code></p>
